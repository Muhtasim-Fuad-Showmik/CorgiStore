import { component$ } from "@builder.io/qwik";

export default component$(() => {
	// const store = useStore({
	// 	scrolled: false,
	// });

	return (
		<div id="products">
            <p class="text-black">Products Grid</p>
        </div>
	);
});
